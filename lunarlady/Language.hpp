#ifndef LL_LANGUAGE_HPP
#define LL_LANGUAGE_HPP

#include <string>

namespace lunarlady {
	std::wstring String(const std::string& iString);
	void LanguageLoadLevel(const std::string& iPath);
}

#endif